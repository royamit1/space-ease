export interface ParkingSpotFilters {
    priceRange?: string
    userId?: string
    latitude?: number
    longitude?: number
    maxDistance?: number
}

export interface Location {
    latitude?: number
    longitude?: number
}
